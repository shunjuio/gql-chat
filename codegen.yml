schema: 'schema.graphql'
documents:
  - 'app/javascript/**/*.jsx'
ignoreNoDocuments: true
generates:
  './app/javascript/gql/':
    preset: 'client'
hooks:
  afterAllFileWrite:
    - yarn generate:pq-manifest
